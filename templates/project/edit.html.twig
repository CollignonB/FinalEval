{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
    <h1>Modifier le Projet {{project.id}}</h1>
    {{ form(form)}}

    <a href="{{ path('project_show', {'id' : project.id}) }}">Retour</a>
{% endblock %}
